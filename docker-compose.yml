services:
  api-gateway:
    image: alpine:3.20
    command: ["sh", "-c", "echo api-gateway ready && sleep infinity"]

  agent-registry:
    image: alpine:3.20
    command: ["sh", "-c", "echo agent-registry ready && sleep infinity"]

  identity:
    image: alpine:3.20
    command: ["sh", "-c", "echo identity ready && sleep infinity"]

  scheduler:
    image: alpine:3.20
    command: ["sh", "-c", "echo scheduler ready && sleep infinity"]

  workforce-orchestrator:
    image: alpine:3.20
    command: ["sh", "-c", "echo workforce-orchestrator ready && sleep infinity"]
